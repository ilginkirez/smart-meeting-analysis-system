"""🔵 Kişi B — Duygu ajanı (LangGraph node). Kişi A'nın emotion modülünü sarar."""

# TODO: Kişi B bu dosyayı geliştirecek


class EmotionAgent:
    """LangGraph node: EmotionFusion'ı saran ajan."""

    def __call__(self, state: dict) -> dict:
        raise NotImplementedError("Kişi B tarafından implement edilecek")
